/**
 * EmailJS configuration for OTP verification emails.
 * Dashboard: https://dashboard.emailjs.com/
 *
 * Create an email template with these variables:
 *   {{email}}  {{user_name}}  {{otp_code}}  {{message}}
 * Set the template "To email" field to {{email}}.
 */
const EMAILJS_CONFIG = {
  serviceId: 'service_x016mmu',
  templateId: 'template_uos5tae',
  publicKey: 'mA-WV0TFP3RLbMJzb'
};
